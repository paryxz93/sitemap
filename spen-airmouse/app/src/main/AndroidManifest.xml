<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      SYSTEM_ALERT_WINDOW e' il ripiego per l'overlay del cursore.
      Il percorso primario e' TYPE_ACCESSIBILITY_OVERLAY, che NON richiede
      alcun permesso runtime perche' la finestra viene aggiunta dal contesto
      dell'AccessibilityService. Vedi service/CursorOverlay.kt.
    -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!--
      L'SDK Pen Remote non parla direttamente con la penna: e' un client AIDL
      che si lega al servizio di sistema Samsung com.samsung.android.service
      .aircommand, il quale tiene lui la connessione BLE. L'app non apre mai un
      GATT proprio, quindi nessun permesso Bluetooth.

      Servono pero' le due dichiarazioni qui sotto. Nessuna delle due e' nella
      documentazione ne' nel progetto di esempio ufficiale, che risale a
      targetSdk 28 e non incontrava il problema. Entrambe sono verificabili nel
      bytecode dell'SDK.
    -->

    <!--
      Senza questo permesso bindService restituisce false e l'SDK logga
      testualmente "Add com.samsung.android.sdk.penremote.BIND_SPEN_REMOTE
      permission" (stringa presente in SpenRemote.startServiceBind).
    -->
    <uses-permission android:name="com.samsung.android.sdk.penremote.BIND_SPEN_REMOTE" />


    <!--
      SpenRemote.isAirCommandExisted() interroga il PackageManager per questo
      pacchetto. Da targetSdk 30 il filtro di visibilita' dei pacchetti lo
      nasconde se non e' dichiarato qui, e l'SDK conclude "This device does not
      support S Pen" su un dispositivo che la supporta. Questo blocco e' la
      differenza fra la penna che funziona e un errore che accusa il telefono.
    -->
    <queries>
        <package android:name="com.samsung.android.service.aircommand" />
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault.DayNight">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|density|keyboard|keyboardHidden|navigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.AirMouseAccessibilityService"
            android:exported="false"
            android:label="@string/service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>
</manifest>
